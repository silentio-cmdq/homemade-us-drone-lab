# Setup 01 firmware

- App: Betaflight Configurator current release.
- Load the **exact AIO target**, then paste `cli-diff-starter.txt` as a starting point, not as law.
- Save a `dump` after the aircraft flies. Commit the dump here with the date.

Also bind ELRS with a unique bind phrase. Do not reuse the default.
